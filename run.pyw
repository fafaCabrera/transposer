# run.pyw
# Windows launcher — .pyw files run via pythonw.exe (no console window).
# Double-click this file to launch the app without a black terminal.
# Usage: pythonw run.pyw [optional_file_path]
import main
main.main()
